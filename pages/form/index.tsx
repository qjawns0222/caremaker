import { NextPage } from "next";
import FirstmakeContainer from "../../containers/form";
const make: NextPage = () => {
  return (
    <div>
      <FirstmakeContainer />
    </div>
  );
};

export default make;
